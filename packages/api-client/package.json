{
  "name": "@nvy/api-client",
  "version": "0.0.0",
  "private": true,
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "api:gen": "openapi-generator-cli generate -i https://api.xiaocaishen.me/v3/api-docs -g typescript-fetch -o src/generated --skip-validate-spec",
    "api:gen:dev": "openapi-generator-cli generate -i http://localhost:8080/v3/api-docs -g typescript-fetch -o src/generated --skip-validate-spec",
    "typecheck": "tsc --noEmit",
    "lint": "echo 'no lint config yet (PR #6)'",
    "lint:fix": "echo 'no lint config yet (PR #6)'",
    "test": "echo 'no tests yet'",
    "test:watch": "echo 'no tests yet'"
  },
  "dependencies": {
    "@nvy/auth": "workspace:*",
    "@nvy/types": "workspace:*"
  },
  "devDependencies": {
    "@nvy/tsconfig": "workspace:*",
    "@openapitools/openapi-generator-cli": "^2.16.4",
    "typescript": "~5.9.0"
  }
}
