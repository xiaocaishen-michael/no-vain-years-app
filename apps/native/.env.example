# Local dev (default for `pnpm web`)
EXPO_PUBLIC_API_BASE_URL=http://localhost:8080

# Cloudflare Pages: set in CF dashboard → Project → Settings → Environment Variables
# Production:  EXPO_PUBLIC_API_BASE_URL=https://api.xiaocaishen.me
